import { ReactNode } from 'react';
import { CardDef } from '@shared/types';
interface Props {
    card: CardDef;
    playerName: string;
    children?: ReactNode;
}
export default function PlayedCardOverlay({ card, playerName, children }: Props): import("react").JSX.Element;
export {};
//# sourceMappingURL=PlayedCardOverlay.d.ts.map