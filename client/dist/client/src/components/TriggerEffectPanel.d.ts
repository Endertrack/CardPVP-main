/**
 * 触发效果提示面板
 * 展示在 PlayedCardOverlay 下方，风格与打出提示框一致。
 * 一行展示一个触发效果（如装备触发、buff 触发、伤害结果等）。
 * 不含绝对定位容器，由父组件控制位置。
 */
export default function TriggerEffectPanel(): import("react").JSX.Element | null;
//# sourceMappingURL=TriggerEffectPanel.d.ts.map